const generateComponent = require('./generateComponent');
const generateContainer = require('./generateContainer');
const init = require('./initRct');

exports.generateComponent = generateComponent;
exports.generateContainer = generateContainer;
exports.init = init;
